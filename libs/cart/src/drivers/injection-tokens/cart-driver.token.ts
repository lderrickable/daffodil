import { InjectionToken } from '@angular/core';

/**
 * Injection token that serves as a placeholder for any service that implements the DaffCartServiceInterface.
 */
export const DaffCartDriver = new InjectionToken('DaffCartDriver');
